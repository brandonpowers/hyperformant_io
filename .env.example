# Database
DATABASE_URL="postgresql://postgres:hyperformant-dev@localhost:5432/hyperformant?schema=public"

# Next.js
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-nextauth-secret-key-here"

# Infisical Secret Management
# Generate service tokens after setting up Infisical via: hyperformant secrets setup
INFISICAL_TOKEN_DEV=""        # Development environment service token
INFISICAL_TOKEN_STAGING=""    # Staging environment service token  
INFISICAL_TOKEN_PROD=""       # Production environment service token

# Google OAuth: https://wasp.sh/docs/auth/social-auth/google
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Microsoft OAuth
MICROSOFT_CLIENT_ID=""
MICROSOFT_CLIENT_SECRET=""

# Stripe Payments
STRIPE_API_KEY="your-stripe-api-key-here"

# LemonSqueezy Payments
LEMONSQUEEZY_API_KEY="your-lemonsqueezy-api-key-here"
# After creating a store, you can find your store id in the store settings https://app.lemonsqueezy.com/settings/stores
LEMONSQUEEZY_STORE_ID=012345
# define your own webhook secret when creating a new webhook on https://app.lemonsqueezy.com/settings/webhooks
LEMONSQUEEZY_WEBHOOK_SECRET="my-webhook-secret"

# Apollo.io Integration (OPTIONAL - will be managed via Infisical)
APOLLO_API_KEY=""             # Apollo.io API Key (migrate to Infisical)
APOLLO_BASE_URL="https://api.apollo.io/v1"
APOLLO_WEBHOOK_SECRET="your-apollo-webhook-secret-here"
APOLLO_WEBHOOK_VERIFY_TOKEN="your-webhook-verify-token-here"

# AI Services (OPTIONAL - will be managed via Infisical)
# These can be left empty after migrating to Infisical secret management
OPENAI_API_KEY=""             # OpenAI API Key for GPT models (migrate to Infisical)
ANTHROPIC_API_KEY=""          # Anthropic API Key for Claude models (migrate to Infisical)  
GOOGLE_AI_API_KEY=""          # Google AI API Key for Gemini models (migrate to Infisical)

# N8N Integration
N8N_HOST="https://hyperformant.app.n8n.cloud"
N8N_WEBHOOK_URL="https://hyperformant.app.n8n.cloud"
N8N_API_KEY="your-n8n-cloud-api-key-here"  # Required for n8n API authentication
N8N_API_ENABLED="true"  # Enable n8n API (for local instances)
N8N_BASIC_AUTH_ACTIVE="false"  # Disable basic auth when using API key (for local instances)

# Email Services (OPTIONAL - will be managed via Infisical)
RESEND_API_KEY=""             # Resend Email API Key (migrate to Infisical)
SMTP_HOST="localhost"
SMTP_PORT="2500"
SMTP_USER=""
SMTP_PASS=""

# Analytics (OPTIONAL - will be managed via Infisical)
PLAUSIBLE_DOMAIN="hyperformant.io"
PLAUSIBLE_API_KEY=""          # Plausible Analytics API Key (migrate to Infisical)
POSTHOG_KEY="your-posthog-key-here"
POSTHOG_HOST="https://app.posthog.com"

# File Storage
AWS_ACCESS_KEY_ID="your-aws-access-key-here"
AWS_SECRET_ACCESS_KEY="your-aws-secret-key-here"
AWS_S3_BUCKET="hyperformant-files"
AWS_REGION="us-east-1"